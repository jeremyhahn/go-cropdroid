
syntax = "proto3";
option go_package = "state";

message ControllerState {
    repeated Metric metrics = 1;
    repeated Channel channels = 2;
    string timestamp = 3;
}

message Metric {
    double key = 1;
    double value = 2;
}

message Channel {
    double id = 1;
    int32 value = 2;
}